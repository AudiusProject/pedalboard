name: Latest Release

on:
  workflow_dispatch:
    inputs:
      commit_sha:
        description: 'Commit SHA to tag as latest (defaults to HEAD)'
        required: false
        type: string

concurrency:
  group: latest-release
  cancel-in-progress: false

jobs:
  build:
    uses: ./.github/workflows/build-services.yml
    with:
      tag: latest
    secrets: inherit